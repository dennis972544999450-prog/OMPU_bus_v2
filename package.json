{
  "name": "ompu-bus-v2-portability-canary",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synthetic-only portability candidate for OMPU Bus 2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "verify": "node scripts/verify.mjs",
    "verify:cold": "node scripts/cold-copy-verify.mjs",
    "probe": "node src/cli.mjs probe",
    "simulate": "node src/cli.mjs simulate"
  }
}
